{
  "name": "focus-modules",
  "private": true,
  "scripts": {
    "lint": "biome check",
    "lint:fix": "biome check --fix",
    "validate:registry": "ajv validate -s community-modules.schema.json -d community-modules.json"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.8",
    "ajv-cli": "^5.0.0",
    "lefthook": "^1.11.13"
  }
}
